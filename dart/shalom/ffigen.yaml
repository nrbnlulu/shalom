# Run with `dart run ffigen --config ffigen.yaml`.
name: ShalomBindings
description: |
  Bindings for `src/shalom.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/shalom_bindings_generated.dart'
headers:
  entry-points:
    - 'src/shalom.h'
  include-directives:
    - 'src/shalom.h'
comments:
  style: any
  length: full
