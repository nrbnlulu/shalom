
{% macro field_to_json_logic(fields) %}
{% for name, field in fields | items %}
    {% set field_ty = resolve_field_type(field.common).ty %}
    {% if field.is_optional and field.default_value is none %}  
        if ({{ name }}.isSome()) {
            {% if field_ty.kind == "InputObject" %} 
                data["{{ name }}"] = {{ name }}.some()?.toJson();   
            {% elif field_ty.kind == "Enum" %}
                data["{{ name }}"] = {{ name }}.some()?.name;
            {% else %}
                data["{{ name }}"] = {{ name }}.some();
            {% endif %}
        } 
    {% else %}
        {% if field_ty.kind == "InputObject" %}
            data["{{ name }}"] = {{ name }}.toJson();
        {% elif field_ty.kind == "Enum" %}
            {% if field.is_optional %}
                data["{{ name }}"] = {{ name }}?.name;
            {% else %}
                data["{{ name }}"] = {{ name }}.name;
            {% endif %}
        {% else %}
            data["{{ name }}"] = {{ name }}; 
        {% endif %}
    {% endif %}
{% endfor %}
{% endmacro %}

{% macro field_from_json_logic(fields, typename) %}
{% for name, field in fields | items %}
    {% set field_ty = resolve_field_type(field.common).ty %}
    {% set concrete_typename = concrete_typename_of_field(field) %}
    {% if field_ty.kind == "InputObject" %} 
        final {{ type_name_for_field(field) }} {{ name }}_value;
        {% if field.is_optional %}  
            final JsonObject? {{ name }}$raw = data['{{ name }}'];
            if ({{ name }}$raw != null) {   
                {% if field.default_value is none %}
                    {{ name }}_value = Some({{ concrete_typename }}.fromJson({{ name }}$raw));   
                {% else %}
                    {{ name }}_value = {{ concrete_typename }}.fromJson({{ name }}$raw);   
                {% endif %}
            } else {
                {% if field.default_value is none %}
                    {{ name }}_value = None();
                {% else %}
                    {{ name }}_value = null;
                {% endif %}
            }
        {% else %}
            {{ name }}_value = {{ concrete_typename }}.fromJson(data['{{ name }}']);   
        {% endif %}
    {% elif field_ty.kind == "Enum" %}
        final {{ type_name_for_field(field) }} {{ name }}_value;
        {% if field.is_optional %}  
            final String? {{ name }}$raw = data['{{ name }}'];
            if ({{ name }}$raw != null) {
                {% if field.default_value is none %}
                    {{ name }}_value = Some({{ concrete_typename }}.fromString({{ name}}$raw));
                {% else %}
                    {{ name }}_value = {{ concrete_typename }}.fromString({{ name}}$raw);
                {% endif %}
            } else {
                {% if field.default_value is none %}
                    {{ name }}_value = None();
                {% else %}
                    {{ name }}_value = null;
                {% endif %}
            }
        {% else %}
                {{ name }}_value = {{ concrete_typename }}.fromString(data['{{ name }}']);
        {% endif %}
    {% else %}
        final {{ type_name_for_field(field) }} {{ name }}_value;
        {% if field.is_optional %}  
            final {{ concrete_typename }}? {{ name }}$raw = data['{{ name }}'];
            if ({{ name }}$raw != null) {
                {% if field.default_value is none %}
                    {{ name }}_value = Some({{ name }}$raw);
                {% else %}
                    {{ name }}_value = {{ name}}$raw;
                {% endif %}
            } else {
                {% if field.default_value is none %}
                    {{ name }}_value = None();
                {% else %} 
                    {{ name }}_value = null;
                {% endif %}
            }
        {% else %} 
                {{ name }}_value = data['{{ name }}'];
        {% endif %}
    {% endif %}
{% endfor %}
    return {{ typename }} (
        {% for name, field in fields | items %}
           {{ name }}: {{ name }}_value,
        {% endfor %}
    );
{% endmacro %}

{% macro field_update_with_json_logic(fields, typename) %}
{% for name, field in fields | items %}
    {% set field_ty = resolve_field_type(field.common).ty %}
    {% set concrete_typename = concrete_typename_of_field(field) %}
    {% if field_ty.kind == "InputObject" %} 
        final {{ type_name_for_field(field) }} {{ name }}_value;
        {% if field.is_optional %}  
            if (data.containsKey('{{ name }}')) {   
                {% if field.default_value is none %}
                    {{ name }}_value = Some({{ concrete_typename }}.fromJson(data['{{ name }}']));   
                {% else %}
                    {{ name }}_value = {{ concrete_typename }}.fromJson(data['{{ name }}']);   
                {% endif %}
            } else {
                {{ name }}_value = {{ name }};
            }
        {% else %}
            {{ name }}_value = {{ concrete_typename }}.fromJson(data['{{ name }}']);   
        {% endif %}
    {% elif field_ty.kind == "Enum" %}
        final {{ type_name_for_field(field) }} {{ name }}_value;
        {% if field.is_optional %}  
            if (data.containsKey('{{ name }}')) {
                {% if field.default_value is none %}
                    {{ name }}_value = Some({{ concrete_typename }}.fromString(data['{{ name }}']));
                {% else %}
                    {{ name }}_value = {{ concrete_typename }}.fromString(data['{{ name}}']);
                {% endif %}
            } else {
                {{ name }}_value = {{ name }};
            }
        {% else %}
                {{ name }}_value = {{ concrete_typename }}.fromString(data['{{ name }}']);
        {% endif %}
    {% else %}
        final {{ type_name_for_field(field) }} {{ name }}_value;
        {% if field.is_optional %}  
            if (data.containsKey({{ name }})) {
                {% if field.default_value is none %}
                    {{ name }}_value = Some(data['{{ name }}']);
                {% else %}
                    {{ name }}_value = data['{{ name }}'];
                {% endif %}
            } else {
                {{ name }}_value = {{ name }};
            }
        {% else %} 
                {{ name }}_value = data['{{ name }}'];
        {% endif %}
    {% endif %}
{% endfor %}
    return {{ typename }} (
        {% for name, field in fields | items %}
           {{ name }}: {{ name }}_value,
        {% endfor %}
    );
{% endmacro %}



{% macro field_initialization_logic(fields) %}
{% for name, field in fields | items %}
    {% if not field.is_optional %}
        {% if field.default_value is not none  %} 
            required this.{{ name }} = {{ parse_field_default_value(field) }}
        {% else %}
            required this.{{name}}  
        {% endif %},
    {% else %}
        {% if field.default_value is not none %} 
            {% set default_value = parse_field_default_value(field) %}
            {% if default_value == "null" %}
                this.{{ name }} 
            {% else %}
                this.{{ name }} = {{ default_value }}
            {% endif %}
        {% else %} 
            this.{{ name }} = const None() 
        {% endif %},
    {% endif %}
    {% endfor %}  
{% endmacro %}