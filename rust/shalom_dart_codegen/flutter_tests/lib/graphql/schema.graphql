type Query {
  user(id: ID!): User
  pet(id: ID!): Pet
  animal(id: ID!): Animal
  zoo(id: ID!): Zoo
  dog(id: ID!): Dog
}

type Subscription {
  shelterAnimals: Animal!
  streetAnimals: Animal!
}

type Zoo {
  id: ID!
  name: String!
  cages: [Cage!]!
}

type Cage {
  id: ID!
  name: String!
}

type User {
  id: ID!
  name: String!
}

type Pet {
  id: ID!
  name: String!
}

interface Animal {
  id: ID!
}

type Location {
  lat: Float!
  lng: Float!
}

type Dog implements Animal {
  id: ID!
  name: String!
  breed: String!
  location: Location
  friend: Animal
}

type Cat implements Animal {
  id: ID!
  name: String!
  color: String!
}
