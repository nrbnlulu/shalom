query GetTask($taskId: ID!) {
  task(id: $taskId) {
    id
    name
    status
  }
}

query GetTaskWithUndecisiveStatus($taskId: ID!) {
  taskWithUndecisiveStatus(id: $taskId) {
    id
    name
    status
  } 
}  