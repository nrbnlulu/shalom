type Query {
  user: User,
  userInfo: UserInfo
  userInfoNotNode: UserInfoNotNode,
}

interface Node {
   id: ID!  
}

type UserInfoNotNode {
   user: User!    
}

type User implements Node {
  id: ID!
  name: String!
  email: String!
  age: Int
}

type Address implements Node {
  id: ID!
  street: String!,
  city: String!
}

type UserInfo implements Node {
  id: ID!,
  user: User!,
  address: Address!
  order: Order!
}

type Order {
  name: String!,
  price: Int!
}   



