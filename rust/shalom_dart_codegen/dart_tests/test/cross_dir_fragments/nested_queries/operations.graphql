query GetAuthor($authorId: ID!) {
    user(id: $authorId) {
        ...AuthorFields
    }
}

query GetPostWithAuthor($postId: ID!) {
    post(id: $postId) {
        ...PostFields
        author {
            ...AuthorFields
        }
    }
}
